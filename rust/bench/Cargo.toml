[package]
name = "confidence_intervals_for_time_series_forecasts_in_python_bench"
description = "Standalone Rust benchmark (no Python)"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "bench"
path = "src/main.rs"

[dependencies]
confidence_intervals_for_time_series_forecasts_in_python_core = { path = "../core" }
